services:
  - type: web
    name: eurusd-signal-api
    runtime: python
    plan: free  # ← CHANGE THIS from "starter" to "free"
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0